import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ryltsov.alex.exif.demo',
  appName: 'Exif Demo',
  webDir: 'www/browser'
};

export default config;
